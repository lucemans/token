name: Deploy to Identity

on:
  push:
    branches:
      - "master"

jobs:
  zip-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: vimtor/action-zip@v1
        with:
          files: identity/web
          dest: identity/result.zip
      - uses: actions/upload-artifact@v2
        with:
          name: identity
          path: ${{ github.workspace }}/identity/result.zip
      - name: Deploy Stage
        uses: fjogeleit/http-request-action@master
        with:
          url: 'https://signal.lvksh.cloud/deployments/push?site=61766271808573440'
          method: 'PUT'
          bearerToken: ${{ secrets.SIGNAL_TOKEN }}
          files: '{ "data": "${{ github.workspace }}/identity/result.zip" }'